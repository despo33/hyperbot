# ===== CONFIGURATION SERVEUR =====
PORT=3002
NODE_ENV=production

# ===== SÉCURITÉ - CHANGEZ CES VALEURS! =====
# Clé secrète pour les tokens JWT (minimum 32 caractères, générez une clé aléatoire)
# Exemple: openssl rand -hex 32
JWT_SECRET=CHANGEZ_MOI_AVEC_UNE_CLE_ALEATOIRE_DE_32_CARACTERES

# Clé de chiffrement AES pour les wallets (exactement 32 caractères)
# Exemple: openssl rand -hex 16
ENCRYPTION_KEY=CHANGEZ_MOI_32_CARACTERES_EXACT

# ===== BASE DE DONNÉES =====
# MongoDB Atlas Connection String
# Format: mongodb+srv://username:password@cluster.xxxxx.mongodb.net/database
MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster.mongodb.net/tradingbot

# ===== SÉCURITÉ AVANCÉE (PRODUCTION) =====

# Activer HSTS (HTTP Strict Transport Security) - nécessite HTTPS
# ENABLE_HSTS=true

# Origines CORS autorisées (séparées par virgule)
# Ex: https://monsite.com,https://app.monsite.com
# CORS_ORIGINS=https://votre-domaine.com

# Forcer l'authentification WebSocket (déconnecte après 10s si non authentifié)
# WS_REQUIRE_AUTH=true

# Liste blanche d'IPs autorisées (séparées par virgule, * = toutes)
# IP_WHITELIST=127.0.0.1,192.168.1.100

# ===== FRONTEND =====
# Mode frontend: 'legacy' (ancien) ou 'vue' (nouveau)
FRONTEND_MODE=legacy
